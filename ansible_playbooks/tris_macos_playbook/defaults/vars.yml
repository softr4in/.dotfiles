## variables for tasks outside of roles

home_user: "trisyong"
# home_group: "staff"

sudoers_custom_config: |
  # Allow users in admin group to use sudo with no password.
  %admin ALL=(ALL) NOPASSWD: ALL

macos_defaults_script: "$HOME/tris_macos_playbook/scripts/macos_defaults.sh --no-restart"
